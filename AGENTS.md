@instructions/AGENTS.md

## Skill Versioning

When editing a skill (files under `skills/*/`), increment the `version` field in that skill's SKILL.md frontmatter. Exception: trivial changes (a one-liner fix or minor wording tweaks across 2-3 lines) do not require a version bump.
